# 0.27.0 local release evidence

Prepared 2026-09-19; local dirty checkout, no push/tag/publication. Package: Beta.

## Local gates

- `make release-check`: passed (compile, lint, typecheck, lockfile, certification
  schema, support matrix, test coverage, release tests, build, wheel/sdist installs,
  optional extras, dependency audits and Twine).
- Full suite: 1206 passed, 16 skipped, 340 subtests; coverage 85.34%.
- Release suite: 73 passed, 10 subtests.
- Optional-extra install checks include `vertex` and Google Auth imports.
- Dependency audits: no known vulnerabilities; no broken installed requirements.
- PostgreSQL-backed tests and protected canaries remain CI gates, not local passes.

## Google smoke on the 0.27.0 wheel

Wheel SHA-256: `c7bbff6992bc322c8344fedfea2b00a35fe7731466794a4a575591e42aa4a183`.
[Exact-wheel smoke](0.27.0-vertex-smoke.json), ADC/global, unchanged runner inputs:
Gemini 3.8 Flash passed generation, streaming, structured output, client tools,
count-tokens and retrieval. Gemma 4 passed generation, streaming and vision but
structured output and tools failed; inspect diagnostics in the report. The combined
matrix is not passed, and this is not protected release certification.

## Documentation and artifacts

216 historical reports were consolidated with original data preserved exactly in
[the historical archive](0.27.0-vertex-integration-history.json). They retain their
original wheel identities and do not certify new artifact bytes. Chronological
sections were removed from the guide; the [scope summary](../providers/vertex-readiness-audit.md)
records remaining gaps. Final distributions and SHA256SUMS are in `dist/`.

`make release-check` succeeded before this final evidence/documentation update;
final distributions are rebuilt and install/metadata checked afterward. No runtime
source changed after those gates. Publication still requires reviewed commit,
protected CI/canaries, matching annotated tag and registry hash verification.
