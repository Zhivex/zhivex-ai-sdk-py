# Combined 0.25.0 candidate evidence

Recorded 2026-09-18 after merging main `9ede305` (PR #45). The combined SDK source
is commit `b37841b`: September provider refresh/native sessions plus Qwen Omni.
Version stays **0.25.0**, which was not tagged or published during preparation.

## Release gates

`make RELEASE_ARTIFACT_FLAGS=--require-postgres-workflow-smoke release-check`
passed with uv 0.12.4, Python 3.14.6, and disposable PostgreSQL 16:

- **1085 tests and 169 subtests passed**, no skipped tests; **85.77% coverage**.
- 73 release-tooling tests and 10 subtests passed.
- Lockfile, compile, Ruff, Mypy/public stub, support matrix, and certification schema passed.
- Fresh wheel/sdist and all optional-extra installs passed, including the required
  installed PostgreSQL workflow smoke.
- Dependency audits found no known vulnerabilities; pip consistency and Twine passed.
- `verify_release_tag.py --tag v0.25.0` passed; no tag was created.

## Exact-wheel local live checks

All checks below used the same isolated installed wheel. They are local
integration evidence, not protected release certification.

- [Qwen Omni report](0.25.0-qwen-omni-installed.json): **15/15 operations passed**,
  including mixed image/audio/video, spatial audio, streaming, reasoning budgets,
  search and agent replay. Installed module bytes were compared to the wheel.
- [OpenAI/Meta canaries](0.25.0-combined-local-canary.json): OpenAI Luna generation
  and agent-tool passed; Meta Contributor generation, streaming, structured output,
  application-supplied retrieval and agent-tool passed. Evidence schema validation passed.

Wheel: `zhivex_ai_sdk-0.25.0-py3-none-any.whl`

SHA256: `dcdf3b835ba175d8a3d0e09bbb5ed6e1d8ddba7f8e86f85a78c98d6b59240ba5`

This supersedes the standalone Omni and provider-refresh candidate hashes.
The earlier [PR #45 report](0.25.0-evidence.md) remains historical. It must not be
used to certify this combined wheel. Inputs were synthetic; credentials and
model responses are excluded from retained evidence.

Final local wheel/sdist hashes are in `dist/SHA256SUMS`. Sdist hashes are kept
outside the source tree because the archive contains this report.

## Remaining publication steps

Review/merge the Omni PR and require the complete main CI matrix. Then create
`v0.25.0` once on the approved main commit. The protected publication workflow
rebuilds and live-tests its own wheel before Trusted Publishing. Compare PyPI
hashes against those workflow artifacts. No release tag or registry upload was
performed here. Follow [the combined release plan](0.25.0.md).
