# 0.25.0 local release evidence

Prepared on 2026-09-18 from main `ae0a01c` plus the 0.25.0 release changes.
The package remains Beta. These are local candidate checks, not a publication or
protected-workflow certification claim.

## Completed checks

- uv 0.12.4 synchronized the locked project with all extras; only the package's
  own version changed in `uv.lock`.
- `make RELEASE_ARTIFACT_FLAGS=--require-postgres-workflow-smoke release-check`
  passed using Python 3.14.6 and a disposable PostgreSQL 16 instance.
- Full suite: **1067 tests and 169 subtests passed**, with no skipped tests in
  the PostgreSQL-enabled run. Coverage: **85.65%** (required: 80%).
- Compile, Ruff, Mypy, public stub, support matrix, certification schema and
  release-tooling checks passed.
- Wheel and sdist installed and ran in fresh environments. All optional extras
  passed, including the required installed PostgreSQL workflow smoke.
- Both dependency audits reported no known vulnerabilities; `pip check` and
  Twine checks passed. Existing dependency deprecation warnings remain.
- The expected `v0.25.0` tag matches package metadata. PyPI's latest version was
  0.24.0 and remote tag v0.25.0 was absent during preparation.

## Qwen Omni installed-wheel smoke

[Redacted exact-wheel integration report](0.25.0-qwen-omni-installed.json):
**15/15 operations passed**. The runner imported an isolated installation,
rejected checkout imports, and compared installed SDK modules with wheel bytes.
The agent check required one tool execution and its result in the final answer.

Covered text, thinking streaming, images, audio, spatial audio, video, mixed media,
mixed streaming, prompted JSON, agent tool replay, web search, and Chat reasoning
budgets with text/audio/search. Inputs were synthetic; reports retain neither
credentials nor model responses.

Wheel: `zhivex_ai_sdk-0.25.0-py3-none-any.whl`

SHA256: `e8977703af8882d43d6e6267038021d838dbd2f32735733af8e3ca56922faf01`

Final distribution hashes are also written to the local `dist/SHA256SUMS` after
building. The sdist contains these notes; its hash is kept outside the source tree
so regenerating this report cannot recursively change its recorded hash.

## Publication boundary

The remote Python 3.11–3.14 and minimum/latest dependency CI matrix must pass for
the reviewed commit. The protected publication job rebuilds and tests its own
exact wheel with OpenAI Luna and Meta Contributor before Trusted Publishing.
Qwen's report remains local installed-wheel integration evidence; it is not a
protected Tier-1 certification. See [the release plan](0.25.0.md).

No release tag or registry upload was performed during preparation. Compare
published hashes with the publication workflow artifacts, not an assumed local
candidate. Rebuilding after code or README changes requires fresh wheel evidence.
