# 0.25.0 local candidate evidence

Historical PR #45 candidate. The [combined Omni candidate report](0.25.0-omni-evidence.md)
supersedes these wheel hashes for the pending 0.25.0 release.

Recorded September 17, 2026 UTC (September 16 local time).

- Source candidate: `25ea28bb01af4b45d205bec418f202bf7f3e6179`.
- Python: 3.14; uv: 0.12.4; distribution maturity: Beta.
- `make release-check`: passed, including lock consistency, compilation, Ruff, Mypy/public stubs, generated support docs and certification schema validation.
- Full suite: **1053 passed, 16 skipped, 161 subtests**, **84.44% coverage**. Skipped integrations are not claimed as executed.
- Release-tooling suite: **73 passed, 10 subtests**.
- Fresh installation checks: wheel, sdist, packaged CLI/skills and configured optional extras passed. Local PostgreSQL live checks remain delegated to CI.
- Dependency security: environment consistency passed; all-extras and local installed-package audits found no known vulnerabilities.
- Twine metadata checks: wheel and sdist passed.
- Additional September live runner: Ruff and compilation passed.

## Exact local artifacts

| Artifact | SHA-256 |
| --- | --- |
| `zhivex_ai_sdk-0.25.0-py3-none-any.whl` | `191d5ca8582d1508c6047109284b4be9b99c0f9a1064aa909ddc9d9d3b1c2de0` |
| `zhivex_ai_sdk-0.25.0.tar.gz` | `719afe7a8111ad8cd1c2dce06d234fc61c6fb8346a26aa6786ff7a7df891b467` |

The artifacts were built from the implementation subsequently committed as the source candidate above. Evidence/report-only follow-up commits do not convert those local bytes into a published release. The protected tag workflow rebuilds, tests and certifies its own exact distributions.

## 0.25.0 installed-wheel canaries

Both required canaries passed against the wheel above using the new strict `0.25.0-smoke-policy.json`:

- OpenAI `gpt-5.6-luna`: generation and agent-tool.
- Meta `muse-spark-1.2-contributor`: generation, streaming, structured output, portable retrieval and agent-tool.

[Machine-readable canary evidence](0.25.0-local-canary.json) passed schema validation. The certification evaluator correctly labels these two targets **integration-only**, because execution was local. Unselected targets are not certified by this canary run. The publish workflow must still produce fresh GitHub Actions evidence and satisfy its protected environment before publishing.

The broader [September operation report](0.24.0-september-live-report.md) remains evidence for its original development wheel: 29/30 operation groups passed, with Lyria 3.5 blocked by HTTP 429. It is retained unchanged as provider integration history, not relabeled as 0.25.0 certification.

## Publication state

PyPI reported 0.24.0 as latest and no 0.25.0 release; remote `v0.25.0` was absent during preparation. Recheck immediately before tagging. No release tag or publication was performed. CI, review and merge remain the preconditions for tagging the approved main commit.
