# 0.24.0 local release evidence

Prepared on 2026-09-06 from main base `c4b88da07b61a02a14a8fc533111832cf9d62de3` plus the uncommitted release changes. This is candidate integration evidence, not evidence for a clean tagged commit or a published artifact. Python 3.14.6; uv 0.12.4 installed temporarily under `/tmp/zhivex-release-uv`.

## Results

- `make release-check`: passed with uv 0.12.4 on PATH and temporary uv/pip caches. Lock verification, compilation, Ruff, Mypy, public stub, certification schema and support matrix passed.
- Full default suite: 1034 passed, 16 PostgreSQL tests skipped, 161 subtests passed; coverage 84.29% (80% required).
- Full suite with a temporary PostgreSQL 16 database: 1050 passed, zero skipped, 161 subtests passed. The container was stopped and removed after validation.
- Release tooling suite: 72 passed, 10 subtests passed.
- Wheel and sdist built and installed in fresh environments; base API/CLI smoke and all configured extras (postgres, mcp, api, a2a, ag-ui, otel, docx, realtime) passed.
- Installed-wheel Stable catalog imports, reviewed pricing, and local storage classification were checked by the release artifact smoke.
- Required installed-wheel Postgres workflow smoke: passed with `--require-postgres-workflow-smoke`.
- `pip check`, all-extras universal dependency audit, and local dependency audit passed; no known vulnerabilities were reported. The local audit excludes the editable project itself.
- Twine accepted both distributions. `verify_release_tag.py --tag v0.24.0` and `git diff --check` passed.
- PyPI latest was 0.23.0; PyPI 0.24.0 and remote tag v0.24.0 were absent at preparation time.

## Candidate SHA-256

| Artifact | SHA-256 |
|---|---|
| `zhivex_ai_sdk-0.24.0-py3-none-any.whl` | `49cc7344e833ff12c1293b8a5a66f1b306727dddf9ef3bed9deaafc9d966549f` |
| `zhivex_ai_sdk-0.24.0.tar.gz` | `c01cf0d54f325fa5d6d04100e5e0a68f36b9a1faba4e2cc37f266a07d008a485` |

## Remaining publication gates

The Python 3.11–3.14 and dependency compatibility CI matrix must pass for the approved main commit. The protected release workflow must build and live-test its own exact wheel using `0.24.0-smoke-policy.json`; no provider live smoke was run during this preparation. Existing 0.23.0 certification records remain historical and do not certify 0.24.0. Tags, push, and publication were not performed.

Follow [the release plan](0.24.0.md). A rebuild may change artifact hashes; compare published bytes with the workflow artifacts, not with this dirty-checkout candidate.
